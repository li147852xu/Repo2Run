# Copy to .env and fill in locally. Do not commit .env.
# Optional: load with `set -a && source .env && set +a` in your shell.

# --- OpenAI-compatible (OpenAI, DeepSeek, Groq, Moonshot, vLLM, Azure OpenAI, …) ---
OPENAI_API_KEY=
# OPENAI_BASE_URL=https://api.deepseek.com

# Optional overrides used only by Repo2Run (override SDK defaults):
# REPO2RUN_OPENAI_API_KEY=
# REPO2RUN_OPENAI_BASE_URL=

# --- Anthropic Claude ---
# ANTHROPIC_API_KEY=

# --- Repo2Run ---
# REPO2RUN_LLM_PROVIDER=auto
